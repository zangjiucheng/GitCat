# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.claude/scratch

# Playwright E2E
.playwright-cli
/test-results
/playwright-report
/blob-report
/output/playwright

# VitePress docs site (built by .github/workflows/docs.yml, not committed)
docs/.vitepress/dist
docs/.vitepress/cache

# `pnpm flamegraph` (scripts/profile.mjs) — samply's own recording output,
# written to the repo root (samply's default -o path is relative to the
# cwd it's invoked from). Local profiling artifacts, never meant to be
# committed.
profile.json.gz
profile.kernel.etl

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
