// Tama's Safety-Manager line when a backup snapshot is pinned (emitted by the
// `snapshot.surfaced` event in legacy/main.ts's TamaMascot). Keys become
// `snapshot.<key>`.
export default {
  surfaced: "Backup {ref} pinned — you're covered.",
};
